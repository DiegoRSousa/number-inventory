apiVersion: v1
kind: Pod
metadata:
  name: pod-oss
  labels:
    app: pod-oss
spec:
  containers:
  - name: pod-nginx
    image: nginx:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
