spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${URL:jdbc:mysql://localhost:3306/number_inventory}
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  sql:
    init:
      mode: always
springdoc:
  api-docs:
    path: /api-docs
      
  
     
   
   